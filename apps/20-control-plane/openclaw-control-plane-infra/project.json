{
  "name": "openclaw-control-plane-infra",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/20-control-plane/openclaw-control-plane-infra/cdk",
  "projectType": "application",
  "tags": ["cdk-app"],
  "targets": {
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["cdk deploy --all --require-approval never"],
        "parallel": false,
        "cwd": "apps/20-control-plane/openclaw-control-plane-infra"
      },
      "configurations": {
        "dev": { "commands": ["DEPLOY_ENV=dev cdk deploy --all --require-approval never"] },
        "prod": { "commands": ["DEPLOY_ENV=prod cdk deploy --all --require-approval never"] }
      }
    },
    "diff": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["cdk diff --all"],
        "parallel": false,
        "cwd": "apps/20-control-plane/openclaw-control-plane-infra"
      },
      "configurations": {
        "dev": { "commands": ["DEPLOY_ENV=dev cdk diff --all"] },
        "prod": { "commands": ["DEPLOY_ENV=prod cdk diff --all"] }
      }
    },
    "synth": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["cdk synth --all"],
        "parallel": false,
        "cwd": "apps/20-control-plane/openclaw-control-plane-infra"
      },
      "configurations": {
        "dev": { "commands": ["DEPLOY_ENV=dev cdk synth --all"] },
        "prod": { "commands": ["DEPLOY_ENV=prod cdk synth --all"] }
      }
    },
    "list": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["cdk list"],
        "parallel": false,
        "cwd": "apps/20-control-plane/openclaw-control-plane-infra"
      },
      "configurations": {
        "dev": { "commands": ["DEPLOY_ENV=dev cdk list"] },
        "prod": { "commands": ["DEPLOY_ENV=prod cdk list"] }
      }
    },
    "destroy": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["cdk destroy --all --force"],
        "parallel": false,
        "cwd": "apps/20-control-plane/openclaw-control-plane-infra"
      },
      "configurations": {
        "dev": { "commands": ["DEPLOY_ENV=dev cdk destroy --all --force"] },
        "prod": { "commands": ["DEPLOY_ENV=prod cdk destroy --all --force"] }
      }
    },
    "bootstrap": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["cdk bootstrap"],
        "parallel": false,
        "cwd": "apps/20-control-plane/openclaw-control-plane-infra"
      },
      "configurations": {
        "dev": { "commands": ["DEPLOY_ENV=dev cdk bootstrap"] },
        "prod": { "commands": ["DEPLOY_ENV=prod cdk bootstrap"] }
      }
    },
    "drift": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["cdk drift --all"],
        "parallel": false,
        "cwd": "apps/20-control-plane/openclaw-control-plane-infra"
      },
      "configurations": {
        "dev": { "commands": ["DEPLOY_ENV=dev cdk drift --all"] },
        "prod": { "commands": ["DEPLOY_ENV=prod cdk drift --all"] }
      }
    }
  }
}
